<div class="row row-cols-3">
  <% @bookmarks.each do |bookmark| %>
    <div class="col">
      <%= render 'shared/movie', bookmark: bookmark %>
    </div>
  <% end %>
</div>

<%= link_to 'Add a movie', new_list_bookmark_path(@list), class: 'btn btn-primary' %>
